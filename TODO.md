# Installation Progress

## TODO Steps:
- [x] 1. Bypass PowerShell execution policy 
- [x] 2. Create Python virtual environment (venv created)
- [x] 3. pip install -r requirements.txt (run in activated venv)
- [x] 4. Backend Node deps installed
- [x] 5. Frontend Node deps installed
- [ ] 6. Run dev servers:
  - Backend: cd backend && npm run dev
  - Frontend: cd frontend && npm run dev
  - Python: source venv/bin/activate (Linux) or venv\\Scripts\\activate.bat (Windows), uvicorn aicore.api:app --reload

